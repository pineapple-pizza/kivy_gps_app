#:include mymapview.kv

BoxLayout:
    orientation: 'vertical'
    MDToolbar:
        title: 'Elevages de chiens'
        right_action_items: [['magnify', lambda x: app.search_menu.open()]]
        md_bg_color: app.theme_cls.primary_color
    MyMapView:
        id: mapview